[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "dlog"
version = "0.1.0"

[tool.setuptools]
py-modules = ["dlog", "storage"]

[tool.setuptools.packages.find]
include = ["commands*"]

[project.scripts]
dlog = "dlog:main"